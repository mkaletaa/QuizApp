import promises from './promises.json'
import async_and_await from './async_and_await.json'
import setTimeout_and_setInterval from './setTimeout_and_setInterval.json'

export const asynchronicity = {
    async_and_await,
    promises,
    setTimeout_and_setInterval,
}